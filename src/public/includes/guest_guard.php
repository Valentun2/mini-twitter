<?php
// includes/guest_guard.php

// Перевіряємо, чи користувач ВЖЕ учасник
if (isset($_SESSION['user_name'])) {
    // Якщо так, перенаправляємо...
    header('Location: /index.php');
    // ...і ТІЛЬКИ ТОДІ зупиняємо скрипт
    exit;
}

// Якщо користувач - гість, цей файл просто
// завершить роботу, і PHP повернеться до login.php
